version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile.prod
    container_name: cgBackend-api
    ports:
      - "8500:8500"
    environment:
      - NODE_ENV=production
    env_file:
      - .env
    restart: always
